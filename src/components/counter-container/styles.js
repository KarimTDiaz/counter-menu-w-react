import styled from 'styled-components';

const StyledCounterContainer = styled.div``;
const CounterButtonsContainer = styled.div`
	display: flex;
`;
export { StyledCounterContainer, CounterButtonsContainer };
